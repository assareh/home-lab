upstream traefik_servers {
{{ range service "traefik" }}
  server {{ .Address }}:{{ .Port }};
  {{else}}
  server 127.0.0.1:65535; # force a 502
{{ end }}
}

server {
    listen 443;
    proxy_pass traefik_servers;
    error_log /var/log/nginx/tcp443.log warn;
}
